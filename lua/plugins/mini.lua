-- ============================================================
-- mini.nvim modules
-- ============================================================

-- mini.pairs (auto pairs)
require("mini.pairs").setup({})

-- mini.surround (add/change/delete surrounding pairs)
require("mini.surround").setup({})

-- mini.ai (better a/i text objects)  ← This is what gives you diq, dib, etc.
require("mini.ai").setup({
    -- You can leave it empty for defaults (recommended)
    -- Or add custom config later if needed
})
